import DefaultLayout from "../Layout/DefaultLayout";

const CustomTimerPage = () => {
    return (
        <DefaultLayout>
            <h1>Custom Timer Page</h1>
        </DefaultLayout>
    );
};

export default CustomTimerPage;