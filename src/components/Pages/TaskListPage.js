import DefaultLayout from "../Layout/DefaultLayout";

const TaskListPage = () => {
    return (
        <DefaultLayout>
            <h1>Task List Page</h1>
        </DefaultLayout>
    );
};

export default TaskListPage;