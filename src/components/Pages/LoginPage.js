import DefaultLayout from "../Layout/DefaultLayout";

const LoginPage = () => {
    return (
        <DefaultLayout>
            <h1>Login Page</h1>
        </DefaultLayout>
    );
};

export default LoginPage;